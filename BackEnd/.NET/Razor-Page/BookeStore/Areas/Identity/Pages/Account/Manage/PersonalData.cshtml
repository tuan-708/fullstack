@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Dữ liệu cá nhân";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h3>@ViewData["Title"]</h3>

<div class="row">
    <div class="col-md-6">
        <p>Tài khoản của bạn chứa dữ liệu cá nhân mà bạn đã cung cấp cho chúng tôi. Trang này cho phép bạn tải xuống hoặc xóa dữ liệu đó.</p>
        <p>
            <strong>
                Việc xóa dữ liệu này sẽ xóa vĩnh viễn tài khoản của bạn và dữ liệu này không thể khôi phục được.
            </strong>
        </p>
        <div class="d-flex">
            <form id="download-data" asp-page="DownloadPersonalData" method="post">
                <button class="btn btn-primary m-2" type="submit">Tải xuống</button>
            </form>
            <p class="m-2">
                <a id="delete" asp-page="DeletePersonalData" class="btn btn-danger">Xóa</a>
            </p>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
